import 'package:flutter/cupertino.dart';

class ImageNoteScreen extends StatelessWidget {
  const ImageNoteScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
